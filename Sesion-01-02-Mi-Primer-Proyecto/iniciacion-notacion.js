// Notacion
// ; -> delimitar el final de una linea
const b = 4;
b + 4;

// . -> se utiliza en los objetos para acceder a los atributos
// movil.anchura

// [] -> listas, arreglos = "Arrays"
const ar = [1, 2, 3, 4]
console.log(ar[0]);

// () -> Funciones 
function suma(a,b){
    // se escribe toda la funcion
}


// {} -> llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}
if (true){
    // todo lo que tenga entre llaves
    const constante2 = "hola"
}

